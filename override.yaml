defaults:
  - config

env:
  kind: alr_dmc
  spec: cheetah_run_dots_background

hydra:
  launcher:
    timeout_min: 30
    cpus_per_task: 10
    gpus_per_node: 1
    name: ${hydra.job.name}
    partition: dev_gpu_4

